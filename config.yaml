# Hermes Agent 配置
# 每个安装者可调整 model/provider（config.yaml 在更新时默认保留本地覆盖）
model:
  model: ""
  provider: ""
  temperature: 0.7

agent:
  max_turns: 500

toolsets:
  - hermes-cli

# 兼容字段：WebUI 欢迎消息
welcome_message: "您好！我是客服助手，有什么可以帮助您的吗？"
